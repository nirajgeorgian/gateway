type Job {
  JobId: String
  jobName: String
}

# extend type Query {
# 	job: Job
# }

input CreateJobReq {
	job_id: String
	job_name: String
}
extend type Mutation {
	CreateJob(input: CreateJobReq!): Job
}
