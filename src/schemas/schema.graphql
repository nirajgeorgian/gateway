scalar DateTime
scalar Date

# basic type for paging and filtering
type PageInfo {
	endCursor: String!
	hasNextPage: Boolean!
}
type Timestamp {
  Seconds: Int
  Nanos: Int
}

type Query {
  dummy: String
}
type Mutation {
  dummy: String
}

extend schema {
  query: Query
  mutation: Mutation
}
